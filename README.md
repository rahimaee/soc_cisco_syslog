# 📘 Cisco Attack Syslog Generator — README (فارسی)

شبیه‌ساز حرفه‌ای لاگ‌های امنیتی Cisco برای SOC و SIEM

این ابزار لاگ‌هایی کاملاً مشابه دستگاه‌های Cisco IOS / ASA / FTD تولید و به SIEM شما ارسال می‌کند.
از این برنامه برای آموزش، آزمایش، ساخت Playbook، تمرین Tier1،‌ توسعه Detection Rule، هشدارسازی و تست Splunk/ELK/Graylog/SIEMها استفاده می‌شود.

## 🚀 امکانات (Features)

- تولید لاگ با فرمت واقعی Cisco Syslog
  مثل:
  ```
  <189>Nov 16 04:34:13 Cisco-Router-Core-1 4232: %SEC-4-PORTSCAN: Port scan detected from 10.8.115.123
  ```

- پشتیبانی از انواع حملات واقعی شبکه

- قابلیت تنظیم شدت، Burst Mode، TCP/UDP، رنج IP، تعداد Thread

- شبیه‌سازی Nmap و اسکن‌های مخفی (FIN, NULL, XMAS)

- کاملاً قابل تنظیم و مناسب سناریوسازی برای SOC

- ارسال مستقیم به SIEM (514/UDP یا 514/TCP)

## 🛠 نصب و اجرا

### 1. پایتون مورد نیاز
Python 3.8+

### 2. اجرای برنامه

فایل را اجرا کنید:

```bash
python cisco_syslog_real_time.py
```

اگر برای اولین بار اجرا می‌کنی، فقط آدرس SIEM را تنظیم کن.

## ⚙️ تنظیمات مهم داخل کد

داخل فایل این موارد را به دلخواه تغییر بده:

### 📍 آدرس SIEM
```python
SIEM_IP = "192.168.1.10"
SIEM_PORT = 514
```

### 📡 نوع پروتکل ارسال
```python
USE_TCP = False   # True = TCP / False = UDP
```

### 💥 شدت ترافیک
```python
INTENSITY = "low"        # low / medium / high / extreme
```

### 🔥 حالت حمله Burst
```python
BURST_MODE = False
BURST_SIZE = 100
```

### 🧵 تعداد Thread
```python
THREADS = 2
```

### 🎯 رنج IP حمله‌کننده
```python
SRC_IP_RANGES = [("10.0.0.0","10.255.255.255")]
```

### 🎯 مقصد اسکن
```python
DST_POOL = ["192.168.1.1", "10.8.115.10"]
```

## 🔥 چه نوع حملاتی تولید می‌کند؟

این ابزار تعداد زیادی حمله واقعی تولید می‌کند. SOC باید هرکدام را تشخیص بدهد.

### 1. 🔶 SYN Scan (Nmap -sS)

حمله بسیار رایج اسکن پورت‌ها.
SOC باید تشخیص دهد:

- تعداد زیاد SYN به پورت‌های مختلف
- بسته بدون Session کامل (بدون ACK)

لاگ مثال:
```
%SEC-6-IPACCESSLOGP: ... SYN scan detected
```

### 2. 🔶 FIN Scan (Nmap -sF)

اسکن مخفی برای دورزدن فایروال‌ها.
SOC باید تشخیص دهد:

- بسته‌ها فقط با پرچم FIN می‌آید
- هیچ Session واقعی ساخته نمی‌شود

### 3. 🔶 NULL Scan (Nmap -sN)

بسته کاملاً بدون Flag.
SOC باید تشخیص دهد:

- TCP بدون ANY Flags
- نشانه واضح تلاش مخفی برای enumeration

### 4. 🔶 XMAS Scan (Nmap -sX)

بسته با پرچم‌های URG + PSH + FIN
SOC باید تشخیص دهد:

- پکت‌های عجیب با چند Flag روشن
- پورت‌های باز/بسته با این روش مشخص می‌شود

### 5. 🔶 Aggressive Scan (Nmap -T4/T5)

اسکن شدید و واضح، مناسب تست Detection های SOC.
SOC باید هشدار بدهد روی:

- تعداد زیاد کانکشن در زمان کوتاه
- پروب پورت‌های پشت سرهم

لاگ نمونه:
```
%FIREWALL-4-SCANDETECT: Aggressive port scan detected
```

### 6. 🔶 UDP Port Sweep

اسکن رنج زیادی از پورت‌های UDP.
SOC باید بررسی کند:

- خطاهای ICMP Port Unreachable زیاد
- ارسال UDP به همه پورت‌ها

### 7. 🔶 ICMP Ping Sweep

اسکن شبکه برای پیدا کردن هاست‌های فعال.
SOC باید تشخیص بدهد:

- تعداد زیاد ICMP Echo Request
- از یک IP به چند مقصد

### 8. 🔶 SSH Brute Force

تلاش زیاد برای ورود به دستگاه Cisco.
SOC باید تشخیص دهد:

- تعداد زیاد Login Failed
- تلاش ورود از یک IP به مدت زیاد

### 9. 🔶 Telnet Brute Force

برای شبکه‌های قدیمی
SOC باید تشخیص دهد:

- لاگ‌های متعدد Telnet Login Failed

### 10. 🔶 DoS / Flood Simulation

شبیه DDoS/DoS روی پورت‌ها
SOC باید تشخیص بدهد:

- ترافیک حجیم در زمان کم
- بسته‌های تکراری زیاد

## 🎯 SOC باید چه چیزهایی یاد بگیرد؟

وقتی این لاگ‌ها را به SIEM ارسال می‌کنی، هدف این است که Tier1 / Tier2 بتواند:

### ✔ تشخیص اسکن پورت

- افزایش ناگهانی اتصال‌ها
- پرچم‌های عجیب در TCP
- یک IP که صدها پورت را اسکن می‌کند

### ✔ تشخیص رفتار Brute Force

- Login Failed پشت سرهم
- زمان و تعداد تلاش‌ها

### ✔ تشخیص رفتار Reconnaissance

- ICMP Sweep
- UDP Sweep
- Aggressive Scan

### ✔ تشخیص حمله DoS

- Packet rate بالا
- پیام‌های پرتکرار در مدت کوتاه

### ✔ ساختن Playbook

- چگونه Log را بررسی کنیم؟
- چه Rule باید در SIEM بسازیم؟
- Alert چه زمانی Critical است؟

### ✔ ساختن Dashboard SOC

- Top attacker IP
- Attack type
- Time histogram
- Event severity

## 📄 بهترین Use Case برای Splunk / ELK / Wazuh

- تست Correlation Search
- تست Notable Alerts (Splunk ES)
- تست Detection Rules
- ساخت Threat Intel Mapping
- آموزش Tier1 برای تشخیص حملات سرورهای واقعی

## ⚠️ نکته مهم

این ابزار فقط لاگ تولید می‌کند
هیچ حمله واقعی انجام نمی‌دهد.
برای استفاده در محیط خارج، نیاز به مجوز قانونی داری.

## 📬 تماس و پشتیبانی

اگر خواستی:

- حملات بیشتری اضافه کنم
- Mode حرفه‌ای‌تر درست کنم
- Dashboard Splunk مخصوص همین لاگ‌ها بسازم
- نسخه Kubernetes / Docker
- نسخه Windows Service

کافیه بگی برات می‌سازم.

"# soc_cisco_syslog" 
